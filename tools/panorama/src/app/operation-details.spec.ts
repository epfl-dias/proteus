import { OperationDetails } from './operation-details';

describe('OperationDetails', () => {
  it('should create an instance', () => {
    expect(new OperationDetails(6, 7, '')).toBeTruthy();
  });
});
