# Run manually to reformat a file:
# clang-format -i --style=file <file>
# 
# Run manually to reformat all files:
#
# find . \( -name '*.cpp' -o -name '*.hpp' -o -name '*.cu' -o -name '*.cuh' \) -exec clang-format -i --style=file '{}' \;
Language: Cpp
BasedOnStyle: Google

---

Language: TableGen
BasedOnStyle: Google
